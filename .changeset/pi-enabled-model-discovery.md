---
"@automatalabs/pi-acp": minor
---

Advertise discovery preferences from merged Pi enabledModels settings using Pi's native pattern resolver, including ordered available model IDs and unmatched patterns. Preserve the complete supported model catalog and refresh preference metadata when configuration changes.
