diff --git a/dist/index.js b/dist/index.js
index f15585a172e7443da1235ad8beb2509cc4925ae7..fb3d25aa1175b281cb279203313cdb4a18a94b07 100755
--- a/dist/index.js
+++ b/dist/index.js
@@ -24786,7 +24786,12 @@ var CodexAcpClient = class {
       summary: disableSummary ? "none" : "auto",
       effort,
       model: modelId.model,
-      serviceTier
+      serviceTier,
+      // agentprism patch: forward a turn-level JSON Schema so the Codex App Server
+      // constrains the FINAL assistant message (turn/start.outputSchema -> Responses
+      // API strict mode). The schema is supplied by @agentprism/acp-agents CodexBackend
+      // via the prompt's _meta["agentprism/outputSchema"]; null when no schema is set.
+      outputSchema: request._meta?.["agentprism/outputSchema"] ?? null
     }, onTurnStarted);
   }
   resolveTurnInterrupted(params) {
