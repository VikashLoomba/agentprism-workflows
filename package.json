{
  "name": "agentprism-workflows",
  "private": true,
  "type": "module",
  "packageManager": "pnpm@10.0.0",
  "scripts": {
    "build": "pnpm -r exec tsc -b",
    "typecheck": "pnpm -r exec tsc --noEmit",
    "test": "pnpm build && pnpm -r test"
  },
  "devDependencies": {
    "@types/node": "^22.10.0",
    "tsx": "^4.22.4",
    "typescript": "^5.7.0"
  },
  "pnpm": {
    "patchedDependencies": {
      "@agentclientprotocol/codex-acp@1.0.2": "patches/@agentclientprotocol__codex-acp@1.0.2.patch"
    }
  }
}
